loadstring(game:HttpGet('https://raw.githubusercontent.com/RENZXW/RENZXW-SCRIPTS/main/Lock'))()

wait(1)

-- Made by RENZXW 
-- https://www.youtube.com/@rexnzxw on YouTube

--[[
hey this is just to see who is using the script; this will be deleted on July 26, 2024
]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Debugging: Check executor variables
print("is_sirhurt_closure:", is_sirhurt_closure)
print("pebc_execute:", pebc_execute)
print("syn:", syn)
print("secure_load:", secure_load)
print("KRNL_LOADED:", KRNL_LOADED)
print("SONA_LOADED:", SONA_LOADED)

-- Executor detection logic (not used in message)
local webhookcheck =
    is_sirhurt_closure and "Sirhurt" or 
    pebc_execute and "ProtoSmasher" or 
    syn and "Synapse X" or 
    secure_load and "Sentinel" or 
    KRNL_LOADED and "Krnl" or 
    SONA_LOADED and "Sona" or 
    "other executer"

local url = "https://discord.com/api/webhooks/1264855302440878100/4XKNRzAAu61cJcGP9wLJ6PoEQYeIzSyIXQqcpd5AnyPNWHRe1tinWWJYhQaoBtBWOYvn"

-- Create a single message combining all the information, excluding "Exploit detected"
local message = "# 🤝RENZXW CAMLOCK SCRIPT USER🤝: # | `Username: " .. LocalPlayer.Name .. "` | `DisplayName: " .. LocalPlayer.DisplayName .. "` | " .. LocalPlayer.Name .. " IS THE OP | He executed the RenzXW lock. | He cool asf. | Join em. | Script: `game:GetService('TeleportService'):TeleportToPlaceInstance(" .. game.PlaceId .. ", '" .. game.JobId .. "')` | Game: https://www.roblox.com/games/" .. game.PlaceId .. "/[1]"

-- Function to send the message to the webhook
local function sendWebhookMessage(message)
    local data = {
        ["content"] = message
    }
    local newdata = game:GetService("HttpService"):JSONEncode(data)

    local headers = {
        ["content-type"] = "application/json"
    }
    local request = http_request or request or HttpPost or syn.request
    local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}

    -- Attempt to send the request
    local success, response = pcall(function()
        return request(abcdef)
    end)

    -- Check for success, but do not print
    if not success then
        -- Optionally handle the error silently
    end
end

-- Send the message
sendWebhookMessage(message)

-- Load the RenzXW Lock script
local success, err = pcall(function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/RENZXW/RENZXW-SCRIPTS/main/RenzXW-Lock.txt'))()
end)

if not success then
    print("Error loading RenzXW-Lock script: " .. err)
end

    local headers = {
        ["content-type"] = "application/json"
    }
    local request = http_request or request or HttpPost or syn.request
    local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}

    -- Attempt to send the request
    local success, response = pcall(function()
        return request(abcdef)
    end)

    -- Check for success, but do not print
    if not success then
        -- Optionally handle the error silently
    end
end

-- Send the message
sendWebhookMessage(message)
-- Send the message
sendWebhookMessage(message)
